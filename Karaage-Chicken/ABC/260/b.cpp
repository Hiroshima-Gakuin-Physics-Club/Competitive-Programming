#include <bits/stdc++.h>
const int INF = 1 << 29;

using namespace std;

int main()
{
    int N, X, Y, Z;
    cin >> N >> X >> Y >> Z;
    vector<int> A(N);
    vector<int> B(N);
    for (int i = 0; i < N; i++)
    {
        cin >> A[i] >> B[i];
    }

    vector<bool> flag(N, false);

    for (int i = 0; i < N; i++)
    {
        int m = 0;
        int n = A[0];
        for (int j = 0; j < N; j++)
        {
            if (!flag[j] &&)
            {
            }
        }
    }
}