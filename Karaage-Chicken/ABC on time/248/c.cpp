#include <bits/stdc++.h>

using namespace std;
//動的計画法を未習得である　勉強しまーす
int N, M, K;
long long INF = 998244353;

long long dfs(int A, int n, int k)
{
    if (n == 0)
    {
    }
}

int main()
{

    cin >> N >> M >> K;
    long long sum = 0;

    cout << dfs(1, N, K) % INF << endl;
}